def tiedostojen_kasittely(filename):
    data = []
    try:
        file = open(filename, 'r')
        for line in lines:
            parts = line.strip().split()

    except ValueError:
        print("Idi nahui")



def main():
    filename = str(input("From which file is the information read?\n"))
    kasittely = tiedostojen_kasittely(filename)
    if
    